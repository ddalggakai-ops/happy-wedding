# 게스트 스냅 (하객 사진 업로드) 연동 방법

하객이 청첩장에서 올린 사진이 **내 구글 드라이브 폴더**에 자동 저장되는 기능입니다.
구글 Apps Script 무료 기능만 사용하며, 하객은 로그인 없이 업로드할 수 있습니다.

## 1. 드라이브 폴더 만들기
1. https://drive.google.com 에서 새 폴더 생성 (예: `결혼식 게스트스냅`)
2. 폴더를 연 상태에서 주소창을 보면
   `https://drive.google.com/drive/folders/여기가_폴더ID`
   → **폴더 ID**를 복사해둡니다.

## 2. Apps Script 만들기
1. https://script.google.com → **새 프로젝트**
2. 기본 코드를 지우고 아래 코드를 붙여넣기 (1번의 폴더 ID로 교체):

```javascript
const FOLDER_ID = "여기에_폴더ID";

function doPost(e) {
  const folder = DriveApp.getFolderById(FOLDER_ID);
  const bytes = Utilities.base64Decode(e.parameter.data);
  const name = (e.parameter.name || "snap.jpg").replace(/[/\\]/g, "_");
  const blob = Utilities.newBlob(bytes, e.parameter.type || "image/jpeg",
    Date.now() + "_" + name);
  folder.createFile(blob);
  return ContentService.createTextOutput("ok");
}
```

3. 저장(💾) 후 오른쪽 위 **배포 → 새 배포**
   - 유형: **웹 앱**
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자**  ← 중요!
4. **배포** 버튼 → 권한 승인 (내 계정 선택 → "고급" → "이동" → 허용)
5. 발급된 **웹 앱 URL**(`https://script.google.com/macros/s/.../exec`) 복사

## 3. 청첩장에 연결
`js/config.js` 에서:

```javascript
snap: {
  appsScriptUrl: "여기에_웹앱_URL",
},
```

푸시하면 청첩장에 "GUEST SNAP" 섹션이 자동으로 나타납니다.
URL이 비어 있으면 섹션이 통째로 숨겨지므로 언제 연결해도 안전합니다.

## 참고
- 파일당 최대 20MB, 한 번에 10장까지 업로드됩니다.
- 코드를 수정한 뒤에는 반드시 **배포 → 배포 관리 → 새 버전**으로 다시 배포해야 반영됩니다.
