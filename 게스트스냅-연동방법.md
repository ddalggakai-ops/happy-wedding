# 포토 빙고 (하객 사진 업로드 + 경품 추첨) 연동 방법

하객이 청첩장에서 3×3 미션 사진을 채워 응모하면
- **사진** → 내 구글 드라이브 폴더 (하객 이름별 하위 폴더)
- **응모 내역** → 내 구글 시트 (이름·연락처·신랑/신부측·빙고 줄 수)

에 자동으로 쌓입니다. 구글 Apps Script 무료 기능만 쓰고, 하객은 로그인이 필요 없습니다.

---

## 1. 드라이브 폴더 만들기

1. https://drive.google.com 에서 새 폴더 생성 (예: `결혼식 포토빙고`)
2. 폴더를 연 상태에서 주소창을 보면
   `https://drive.google.com/drive/folders/여기가_폴더ID`
   → **폴더 ID**를 복사해둡니다.

## 2. 구글 시트 만들기 (응모자 명단 · 추첨용)

1. https://sheets.google.com 에서 새 스프레드시트 생성 (예: `포토빙고 응모자`)
2. 주소창의 `https://docs.google.com/spreadsheets/d/여기가_시트ID/edit`
   → **시트 ID**를 복사해둡니다.

> 시트를 안 쓰고 사진만 모으고 싶다면 `SHEET_ID`를 빈 문자열로 두면 됩니다.

## 3. Apps Script 만들기

1. https://script.google.com → **새 프로젝트**
2. 기본 코드를 지우고 아래 코드를 붙여넣은 뒤, 위쪽 두 줄의 ID를 바꿔주세요.

```javascript
const FOLDER_ID = "1u82O_cmM_Mw-Hid5NK7jyUv8p_RLHFza";
const SHEET_ID  = "16mJSEIHhp0BDr1Rnl1AJYzutL8By1bNBcmrm4JGhdHE";
const RSVP_TAB  = "참석여부";        // 참석 의사가 쌓일 탭 이름 (없으면 자동 생성)

function guestFolderName(p) {
  const name = String(p.guest || "이름없음").trim() || "이름없음";
  const phone = String(p.phone || "").replace(/[^0-9]/g, "");
  const label = phone ? name + "-" + phone : name;
  return label.replace(/[\/\\:*?"<>|]/g, "_").slice(0, 60);
}

function getGuestFolder(label) {
  const lock = LockService.getScriptLock();
  lock.waitLock(30000);
  try {
    const root = DriveApp.getFolderById(FOLDER_ID);
    const found = root.getFoldersByName(label);
    return found.hasNext() ? found.next() : root.createFolder(label);
  } finally { lock.releaseLock(); }
}

function sheetRow(p, fileUrl) {
  if (!SHEET_ID) return;
  const lock = LockService.getScriptLock();
  lock.waitLock(30000);
  try {
    const sh = SpreadsheetApp.openById(SHEET_ID).getSheets()[0];
    if (sh.getLastRow() === 0) {
      sh.appendRow(["시각", "구분", "이름", "연락처", "측", "빙고줄", "사진수", "미션", "사진링크"]);
    }
    sh.appendRow([new Date(), p.kind || "", p.guest || "", p.phone || "", p.side || "",
                  p.lines || "", p.photos || "", p.mission || p.missions || "", fileUrl || ""]);
    SpreadsheetApp.flush();
  } finally { lock.releaseLock(); }
}

/* 참석 의사 — 같은 시트의 별도 탭에 기록 */
function rsvpRow(p) {
  const lock = LockService.getScriptLock();
  lock.waitLock(30000);
  try {
    const ss = SpreadsheetApp.openById(SHEET_ID);
    let sh = ss.getSheetByName(RSVP_TAB);
    if (!sh) {
      sh = ss.insertSheet(RSVP_TAB);
      sh.appendRow(["시각", "이름", "측", "참석여부", "인원", "식사"]);
      sh.setFrozenRows(1);
      sh.getRange("A1:F1").setFontWeight("bold");
      sh.setColumnWidths(1, 6, 120);
    }
    sh.appendRow([new Date(), p.guest || "", p.side || "", p.attend || "",
                  Number(p.count) || 0, p.meal || ""]);
    SpreadsheetApp.flush();
  } finally { lock.releaseLock(); }
}

function mergeDuplicateFolders() {
  const root = DriveApp.getFolderById(FOLDER_ID);
  const byName = {};
  const it = root.getFolders();
  while (it.hasNext()) {
    const f = it.next(); const n = f.getName();
    if (!byName[n]) byName[n] = [];
    byName[n].push(f);
  }
  let moved = 0, removed = 0;
  Object.keys(byName).forEach(function (n) {
    const list = byName[n];
    if (list.length < 2) return;
    const keep = list[0];
    for (let i = 1; i < list.length; i++) {
      const fs = list[i].getFiles();
      while (fs.hasNext()) { fs.next().moveTo(keep); moved++; }
      list[i].setTrashed(true); removed++;
    }
  });
  return "사진 " + moved + "개 이동, 중복 폴더 " + removed + "개 정리";
}

/* 연결 진단
   ?test=1  → 시트에 테스트 행을 한 줄 써봅니다
   ?merge=1 → 중복으로 생긴 하객 폴더를 하나로 합칩니다 */
function doGet(e) {
  const p = (e && e.parameter) || {};
  const out = { ok: true, now: Utilities.formatDate(new Date(), "Asia/Seoul", "MM-dd HH:mm") };
  try {
    const f = DriveApp.getFolderById(FOLDER_ID);
    out.folder = f.getName();
    let files = 0, subs = 0;
    const it = f.getFolders();
    while (it.hasNext()) { subs++; const g = it.next().getFiles(); while (g.hasNext()) { g.next(); files++; } }
    const rf = f.getFiles();
    while (rf.hasNext()) { rf.next(); files++; }
    out.guestFolders = subs; out.fileCount = files;
  } catch (err) { out.ok = false; out.folderError = String(err); }
  try {
    const ss = SpreadsheetApp.openById(SHEET_ID);
    out.sheet = ss.getSheets()[0].getName();
    out.sheetRows = ss.getSheets()[0].getLastRow();
    const rs = ss.getSheetByName(RSVP_TAB);
    out.rsvpRows = rs ? rs.getLastRow() : 0;
  } catch (err) { out.ok = false; out.sheetError = String(err); }
  if (p.test === "1") {
    try { sheetRow({ kind: "test", guest: "연결테스트", mission: "시트 쓰기 테스트" }, ""); out.sheetWrite = "성공"; }
    catch (err) { out.ok = false; out.sheetWrite = "실패: " + String(err); }
  }
  if (p.merge === "1") {
    try { out.merged = mergeDuplicateFolders(); }
    catch (err) { out.ok = false; out.mergeError = String(err); }
  }
  return json(out);
}

function doPost(e) {
  const out = { ok: true };
  const p = (e && e.parameter) || {};

  // 참석 의사 — 사진 없이 시트에만 기록
  if (p.kind === "rsvp") {
    try { rsvpRow(p); }
    catch (err) { out.ok = false; out.error = String(err); }
    return json(out);
  }

  let fileUrl = "";
  try {
    if (p.data) {
      const folder = getGuestFolder(guestFolderName(p));
      const safeName = String(p.name || "photo.jpg").replace(/[\/\\]/g, "_");
      const blob = Utilities.newBlob(
        Utilities.base64Decode(p.data), p.type || "image/jpeg",
        Utilities.formatDate(new Date(), "Asia/Seoul", "MMdd_HHmmss") + "_" + safeName
      );
      const file = folder.createFile(blob);
      if (p.mission) file.setDescription(p.mission);
      fileUrl = file.getUrl();
      out.url = fileUrl;
    }
  } catch (err) { out.ok = false; out.error = String(err); }

  try { sheetRow(p, fileUrl); } catch (err) { out.sheetError = String(err); }
  return json(out);
}

function json(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}
```

### ⚠ 재배포는 반드시 "기존 배포 수정"으로

코드를 고친 뒤 **배포 → 새 배포**를 누르면 **URL이 새로 발급**되고, 청첩장이 쓰는 예전 URL은
계속 옛날 코드를 실행합니다. 겉으로는 잘 되는 것처럼 보이는데 사진이 안 쌓이는 전형적인 원인입니다.
반드시 **배포 → 배포 관리 → ✏️(연필) → 버전: 새 버전 → 배포** 로 같은 URL을 갱신하세요.

배포한 뒤 **웹앱 주소를 브라우저에 그대로 붙여넣어** 보세요.
`{"ok":true,"folder":"...","fileCount":12,"sheet":"...","sheetRows":8}` 처럼
폴더·시트 이름과 **지금까지 쌓인 개수**가 보이면 정상입니다.
`Script function not found: doGet` 이 뜨면 그 URL은 옛날 코드를 실행하는 배포입니다.
`ok:false` 와 함께 `folderError` / `sheetError` 가 나오면 그 ID가 잘못된 것이고,
그 상태에서는 업로드가 조용히 실패하니 ID부터 고쳐주세요.

> 추첨은 시트를 보고 직접 하시면 됩니다. `구분` 열이 **bingo-entry** 인 행이 응모 한 건이고,
> `빙고줄` 열의 숫자가 그 하객이 완성한 줄 수(= 응모권 수)입니다.

3. 저장(💾) 후 오른쪽 위 **배포 → 새 배포**
   - 유형: **웹 앱**
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자**  ← 중요!
4. **배포** 버튼 → 권한 승인 (내 계정 선택 → "고급" → "이동" → 허용)
5. 발급된 **웹 앱 URL**(`https://script.google.com/macros/s/.../exec`) 복사

## 4. 청첩장에 연결

`js/config.js` 에서:

```javascript
snap: {
  appsScriptUrl: "여기에_웹앱_URL",
},
```

푸시하면 청첩장에 **PHOTO BINGO** 섹션(미리보기 판 + "만들러 가기" 버튼)이 나타납니다.
URL이 비어 있으면 섹션이 통째로 숨겨지므로, 예식 직전에 연결해도 안전합니다.

## 4-1. QR 코드용 주소

포토 빙고는 **별도 페이지**입니다. 종이·현수막에 넣을 QR은 아래 주소로 만드세요.

```
https://hyeongkyu-and-hyunjin.life/bingo.html
```

특정 하객 이름을 미리 채우고 싶다면 `?to=홍길동` 을 붙이면 됩니다.
청첩장에서 버튼으로 넘어갈 때는 초대장 URL의 이름이 자동으로 따라갑니다.

## 5. 미션 문구 바꾸기

같은 `js/config.js` 의 `bingo.cells` 아홉 줄이 왼쪽 위부터 순서대로 각 칸입니다.
`icon`(이모지)과 `text`(문구)만 바꾸면 되고, 개수는 반드시 9개를 유지해주세요.

```javascript
bingo: {
  title: "포토 빙고",
  desc: "...",
  prize: "추첨 후 개별 연락드립니다",
  cells: [
    { icon: "🤵", text: "신랑 형규와 한 컷" },
    ...
  ],
},
```

---

## 참고

- 사진은 하객별로 **"이름-연락처" 폴더** 안에 모입니다. 그 하객이 올린 미션 사진, 자유 업로드 사진,
  빙고판 콜라주가 모두 같은 폴더에 들어갑니다.
- 시트 기록이 실패해도 사진은 그대로 저장되고, 응답에 `sheetError` 가 담겨 옵니다.
- 사진은 4장씩 동시에 올라가는데, 그때 폴더가 여러 개 생기거나 시트 행이 덮어써지지 않도록
  폴더 생성과 시트 쓰기를 **LockService 로 잠가** 한 번에 하나씩만 처리합니다.
- **참석 의사(RSVP)** 도 같은 웹앱으로 전송되어, 같은 스프레드시트의 **`참석여부` 탭**에 쌓입니다
  (탭이 없으면 자동으로 만들어집니다).
- 포토 빙고에서 담은 사진은 브라우저에 임시 저장(IndexedDB)되어 **새로고침해도 사라지지 않습니다.**
- 진단용 주소: `웹앱주소?test=1` (시트에 테스트 행 쓰기) / `웹앱주소?merge=1` (중복 하객 폴더 합치기)
- 사진은 브라우저에서 **긴 변 1280px / JPEG** 로 줄여 4장씩 동시에 보냅니다. 원본보다 훨씬 빨리 올라가고,
  드라이브 용량도 아낍니다.
- 응모 조건은 **한 줄(가로·세로·대각선) 완성**입니다. 칸을 더 채우면 응모권이 늘어납니다.
- 빙고에 참여하지 않는 하객도 섹션 맨 아래 **"📷 사진 올리기"** 로 한 번에 100장까지 보낼 수 있습니다
  (시트에는 `구분` 열이 **free-photo** 로 기록됩니다).
- 응모를 마치면 아홉 칸을 한 장으로 합친 **빙고판 콜라주**가 화면에 뜨고, 하객이 저장할 수 있습니다.
  같은 이미지가 드라이브에도 `bingo-collage` 로 함께 저장됩니다.
- 하객이 응모를 마치면 브라우저에 기록이 남아 완료 화면이 표시되며, "다시 응모하기"로 재응모할 수 있습니다.
- 코드를 수정한 뒤에는 반드시 **배포 → 배포 관리 → ✏️ → 버전: 새 버전 → 배포** 로 다시 배포해야 반영됩니다.
- 웹 앱 URL은 청첩장 소스에 그대로 들어갑니다. 사진 추가와 시트 기록만 가능한 엔드포인트라
  노출되어도 드라이브의 다른 파일에는 접근할 수 없지만, 예식이 끝나면 **배포 보관취소**로 닫아두는 편이 좋습니다.
